# test_orange

démarche de résolution du problème
 -création du fichier input.txt qui contient ma puzzle
 -utiliser un input reader pour lire la data
 -stocker la data dans un BufferedReader pour pouvoir la manipuler
 -traitement ligne par ligne (split et parseInt) 
 -boucle de test pour chaque ligne pour vérifier l'écart entre les levels et leur sens
 -afficher les lignes avec leur état (safe ou unsafe) et le nombre de ligne safe

compilation:
    javac Test.java 

exécution:
    java Test.java < input.txt 
